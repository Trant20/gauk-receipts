<section id="how-it-works" style="position: relative; z-index: 10; background: #1b1b1b; padding: clamp(80px,12vh,140px) clamp(20px,6vw,80px) clamp(60px,10vh,120px);">

  <h2 style="font-family: var(--font-serif); font-weight: 400; font-size: clamp(28px,3.8vw,56px); color: var(--color-text); text-align: center; margin-bottom: clamp(48px,8vh,96px); letter-spacing: -0.01em; line-height: 1.1;">3 steps to proof of purchase</h2>

  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px; max-width: 1100px; margin: 0 auto;">

    <div style="border: 1px solid var(--color-border-card); border-radius: var(--radius); padding: clamp(32px,4vw,52px) clamp(24px,3vw,40px); display: flex; flex-direction: column; gap: 18px;">
      <span style="font-family: var(--font-sans); font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--color-sage);">01</span>
      <h3 style="font-family: var(--font-serif); font-weight: 500; font-size: clamp(20px,2vw,26px); color: var(--color-text); line-height: 1.15;">Scan Your Receipt</h3>
      <p style="font-family: var(--font-sans); font-size: clamp(13px,1.1vw,15px); color: var(--color-text-dim); line-height: 1.75;">Point your camera at any receipt. We strip metadata from every photo before it leaves your device. Works with any retailer, any format, any country.</p>
    </div>

    <div style="border: 1px solid var(--color-border-card); border-radius: var(--radius); padding: clamp(32px,4vw,52px) clamp(24px,3vw,40px); display: flex; flex-direction: column; gap: 18px;">
      <span style="font-family: var(--font-sans); font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--color-sage);">02</span>
      <h3 style="font-family: var(--font-serif); font-weight: 500; font-size: clamp(20px,2vw,26px); color: var(--color-text); line-height: 1.15;">GAUK Reads Everything</h3>
      <p style="font-family: var(--font-sans); font-size: clamp(13px,1.1vw,15px); color: var(--color-text-dim); line-height: 1.75;">GAUK extracts every item, price, date and merchant from your receipt. Sale prices, quantities, warranty periods and tax all captured automatically.</p>
    </div>

    <div style="border: 1px solid var(--color-border-card); border-radius: var(--radius); padding: clamp(32px,4vw,52px) clamp(24px,3vw,40px); display: flex; flex-direction: column; gap: 18px;">
      <span style="font-family: var(--font-sans); font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--color-sage);">03</span>
      <h3 style="font-family: var(--font-serif); font-weight: 500; font-size: clamp(20px,2vw,26px); color: var(--color-text); line-height: 1.15;">Your Encrypted Vault</h3>
      <p style="font-family: var(--font-sans); font-size: clamp(13px,1.1vw,15px); color: var(--color-text-dim); line-height: 1.75;">Every receipt saved, searchable and encrypted forever. Filter by merchant, item or date. At claim time your proof of purchase is one search away.</p>
    </div>

  </div>

</section>
